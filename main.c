#include "sequence.h"


int main(int argc, char const *argv[])
{
	t_sentence *s;
	s = NULL;
	//t_queue *q;
	//q = NULL;
	system("clear");
	//sentenceMenu(&s);
	//queueMenu(&q);
	sequenceMenu();
	return 0;
}
